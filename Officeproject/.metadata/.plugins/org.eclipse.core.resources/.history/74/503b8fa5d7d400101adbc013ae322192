package com.cog.pract.controller;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.cog.pract.model.VehicleOwner;

@RestController
@RequestMapping("/vehicleowner")
public class VehicleOwnerController {
	@GetMapping("/vehicle")
    public String sayHello() {
        return "Hello from Spring Boot!";
    }

	@PostMapping("/vehicledetails/{vehclenumber}")
	public void updateStudent(@PathVariable("vehclenumber") String id, @RequestBody VehicleOwner vehicleOwner) {
	    // Update the student record here.
	}
	
}
